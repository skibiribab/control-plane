name: pr-review (ai-coder reusable)

on:
  workflow_call:
    inputs:
      repo:
        type: string
        required: true
        description: Target repository (owner/name)
      pr_number:
        type: string
        required: true
        description: PR to review
      approve:
        type: boolean
        required: false
        default: false
        description: Allow approving the PR when confident
    secrets:
      PAT_TOKEN:
        required: true
        description: Fine-grained PAT with Contents+Issues read/write on the target repo

permissions:
  contents: read
  pull-requests: write

jobs:
  review-pr:
    runs-on: ubuntu-latest
    if: github.repository != 'gardusig/control-plane'
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.repo }}
          token: ${{ secrets.PAT_TOKEN }}
          ref: refs/pull/${{ inputs.pr_number }}/head

      # ai marker: pr-review-gh
      # TODO(ai): only when checks are green, post a review summary; flag issues,
      # never silently approve unless approve=true and confident. Skip re-review.
      - name: AI stub — review PR
        run: |
          echo "AI_STAGE=pr-review"
          echo "REPO=${{ inputs.repo }}"
          echo "PR=${{ inputs.pr_number }}"
          echo "APPROVE=${{ inputs.approve }}"
